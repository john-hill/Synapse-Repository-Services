
name: main

on:
  pull_request:
    branches: ['*']
  push:
    branches: ['*']


jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Hello World
        run: echo Hellow World
