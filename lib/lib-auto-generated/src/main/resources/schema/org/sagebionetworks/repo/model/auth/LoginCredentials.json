{
	"title": "LoginCredentials", 
	"description": "Used to login via username and password",
    "properties": {
		"email": {
			"type": "string",
			"description": "This field is deprecated and should no longer be used. A user is no longer required to log-in with an email address.  Use the login field."
		},
		"login": {
			"type": "string",
			"description": "A user can login with their principalName, email address or open ID."
		},
		"password": {
			"type": "string",
			"description": "The user's password"
		}
	}
}