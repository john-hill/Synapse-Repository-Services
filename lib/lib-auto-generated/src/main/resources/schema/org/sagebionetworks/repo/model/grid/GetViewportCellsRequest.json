{
	"description": "Start a job to get the cells within a viewport",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.asynch.AsynchronousRequestBody"
		}
	],
	"properties": {
		"viewPort": {
			"description": "The ID of a fileHandle of a CSV that defines the starting state of the grid.  The first line of the CSV must be a header that defines the column names of the grid.",
			"$ref":"org.sagebionetworks.repo.model.grid.Viewport"
		}
	}
}
