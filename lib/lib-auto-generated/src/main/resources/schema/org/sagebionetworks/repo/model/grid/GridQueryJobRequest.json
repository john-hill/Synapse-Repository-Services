{
	"description": "Asynchronous job request to query a grid session and return a single page of rows with optional per-row validation data. Does not require a WebSocket connection.",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.asynch.AsynchronousRequestBody"
		}
	],
	"properties": {
		"sessionId": {
			"type": "string",
			"description": "Required. The grid session ID to query."
		},
		"replicaId": {
			"type": "integer",
			"description": "Required. The caller's replica ID. Used for row-selection filtering and must be owned by the caller."
		},
		"queryRequest": {
			"$ref": "org.sagebionetworks.repo.model.grid.query.QueryRequest",
			"description": "Required. The structured query to execute."
		}
	}
}
