{
	"title": "ViewPort",
	"description": "Defines a rectangular view of a data grid.",
	"type": "object",
	"properties": {
		"topLeftAddress": {
			"description": "The address of the top-left cell of the viewport.",
			"$ref": "org.sagebionetworks.repo.model.grid.CellAddress"
		},
		"width": {
			"type": "integer",
			"description": "The width of the viewport (number of columns). Minimum value: 1."
		},
		"height": {
			"type": "integer",
			"description": "The height of the viewport (number of rows). Minimum value: 1."
		},
		"sorting": {
			"type": "array",
			"description": "An array of sorting criteria, applied in order.",
			"items": {
				"$ref": "org.sagebionetworks.repo.model.grid.SortItem"
			}
		}
	}
}
