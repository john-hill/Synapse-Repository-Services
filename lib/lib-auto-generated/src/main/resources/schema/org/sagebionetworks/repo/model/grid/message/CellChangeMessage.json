{
	"description": "Cell change messages is generated each time a cell is changed.",
	"properties": {
		"cellId": {
			"description": "The ID of a cell that changed.",
			"$ref": "org.sagebionetworks.repo.model.grid.CellId"
		},
		"changedById": {
			"type": "string",
			"description": "The ID of the user that made this change"
		},
		"changedOn": {
			"type": "string",
			"format": "date-time",
			"description": "The date-time when this change was made."
		},
		"agentAssist": {
			"type": "boolean",
			"description": "When true, this change was made by an agent on the changeById user's behalf."
		}
	}
}
