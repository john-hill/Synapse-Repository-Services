{
	"description": "A cell validation state change message is generated when a cell's validation state changes.",
	"properties": {
		"cellId": {
			"description": "The ID of a cell that changed.",
			"$ref": "org.sagebionetworks.repo.model.grid.CellId"
		},
		"isValid": {
			"type": "boolean",
			"description": "Was the cell valid at the time this message was generated?"
		},
		"validatedOn": {
			"type": "string",
			"format": "date-time",
			"description": "The date-time when this cell was validated."
		}
	}
}
