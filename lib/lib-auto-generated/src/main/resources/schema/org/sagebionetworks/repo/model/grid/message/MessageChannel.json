{
	"type": "string",
	"description": "The available message channels.",
	"enum": [
		{
			"name": "CELL_CHANGE",
			"description": "Channel for cell level change messages."
		},
		{
			"name": "CELL_VALIDATION_STATE_CHANGE",
			"description": "Channel for cell level validation state change messages."
		}
	]
}
