{
	"description": "Will translate to: COUNT(DISTINCT column_name,[column_name...])",
	"type": "object",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.grid.query.SelectItem"
		}
	],
	"properties": {
		"columnNames": {
			"type": "array",
			"description": "Optional. One or more column name.",
			"items": {
				"type": "string"
			}
		}
	}
}
