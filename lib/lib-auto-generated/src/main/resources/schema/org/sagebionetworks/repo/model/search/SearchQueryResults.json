{
	"description": "Results of a search query against an OpenSearch index. The set of populated fields is governed by the request's responseParts: hits and offset are populated by default; the remaining fields are populated only when the corresponding SearchQueryPart is requested.",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.asynch.AsynchronousResponseBody"
		}
	],
	"properties": {
		"hits": {
			"type": "array",
			"description": "Matching documents. Populated by default (or whenever SearchQueryPart.HITS is requested).",
			"items": {
				"$ref": "org.sagebionetworks.repo.model.search.SearchHit"
			}
		},
		"totalHits": {
			"type": "integer",
			"description": "The total number of matching documents. Populated when SearchQueryPart.TOTAL_HITS is requested."
		},
		"selectColumns": {
			"type": "array",
			"description": "The columns represented in each hit's fields, in SELECT-clause order. Populated when SearchQueryPart.SELECT_COLUMNS is requested. When <code>body._source.includes</code> is non-empty, this is the subset of columns matching those names.",
			"items": {
				"$ref": "org.sagebionetworks.repo.model.table.SelectColumn"
			}
		},
		"aggregationResults": {
			"type": "object",
			"description": "The raw <a href=\"https://docs.opensearch.org/latest/aggregations/\">OpenSearch aggregations</a> response &mdash; the contents of the <code>aggregations</code> key returned by AOSS, with field references rewritten back to column names. Populated whenever the request supplied <code>body.aggregations</code> (or <code>body.aggs</code>); not gated by SearchQueryPart. Returned as an opaque JSON object because the OpenSearch DSL produces a wide variety of bucket shapes per aggregation type and per sub-aggregation."
		},
		"suggestResults": {
			"type": "object",
			"description": "The raw OpenSearch suggester response &mdash; the contents of the <code>suggest</code> key returned by AOSS, with field references rewritten back to column names. Populated when <code>body.suggest</code> was supplied. See the <a href=\"https://docs.opensearch.org/latest/search-plugins/searching-data/did-you-mean/\">term/phrase</a> and <a href=\"https://docs.opensearch.org/latest/search-plugins/searching-data/autocomplete/\">completion</a> suggesters."
		},
		"nextSearchAfter": {
			"type": "array",
			"description": "Opaque cursor for the next <a href=\"https://docs.opensearch.org/latest/search-plugins/searching-data/paginate/\">page</a>. Populated whenever the response carries hits and the underlying sort yielded sort values for the last hit. Pass back unchanged on the next request as <code>body.search_after</code>. Null when there are no further pages.",
			"items": {
				"type": "object"
			}
		},
		"offset": {
			"type": "integer",
			"description": "Zero-based pagination offset echoed from the request. Always populated."
		}
	}
}
