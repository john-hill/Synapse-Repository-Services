{
	"description": "An <a href=\"https://docs.opensearch.org/latest/query-dsl/term/exists/\"><code>exists</code></a> term-level clause. Matches documents that have any non-null value for the given column.",
	"properties": {
		"field": {
			"type": "string",
			"description": "Required. The column that must have a value."
		},
		"boost": {
			"type": "number",
			"description": "Optional. Multiplier for the relevance score of this clause. Default <code>1.0</code>."
		},
		"_name": {
			"type": "string",
			"description": "Optional. Label echoed back in matched-queries metadata."
		}
	}
}
