{
	"description": "A <a href=\"https://docs.opensearch.org/latest/aggregations/metric/minimum/\"><code>min</code></a> metric aggregation &mdash; the minimum value of a numeric column.",
	"implements": [
		{ "$ref": "org.sagebionetworks.repo.model.search.dsl.MetricAggregation" },
		{ "$ref": "org.sagebionetworks.repo.model.search.dsl.MissingValueOption" }
	],
	"properties": {
		"value_type": {
			"type": "string",
			"description": "Optional. Explicit value type for the field when it cannot be inferred."
		}
	}
}
