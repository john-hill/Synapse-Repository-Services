{
	"description": "A shareable bundle of per-column analyzer assignments. Wires up OpenSearch's per-field <a href='https://docs.opensearch.org/latest/field-types/supported-field-types/text/'>analyzer + search_analyzer</a> mapping for specific columns of a SearchIndex, overriding the SearchConfiguration's defaults. Example assigning a strict acronym analyzer to the 'disease_code' column and a fuzzy scientific analyzer to 'authors':\n\n  {\n    \"organizationName\": \"biomed\",\n    \"name\": \"publication_columns\",\n    \"overrides\": [\n      { \"columnName\": \"disease_code\", \"indexAnalyzer\": \"biomed-acronym_exact\",    \"searchAnalyzer\": \"biomed-acronym_exact\" },\n      { \"columnName\": \"authors\",      \"indexAnalyzer\": \"biomed-scientific_search\", \"searchAnalyzer\": \"biomed-scientific_search\" }\n    ]\n  }\n\nReferenced from a SearchConfiguration's columnAnalyzerOverrides by qualified name '{organizationName}-{name}'. Entries whose 'columnName' is not present in a given SearchIndex's schema are silently skipped — a single override resource can therefore apply to multiple indexes that share some column names. Cannot be deleted while referenced by any SearchConfiguration.",
	"properties": {
		"id": {
			"type": "string",
			"description": "The unique ID of this column analyzer override."
		},
		"organizationName": {
			"type": "string",
			"description": "The Organization that owns this resource. Immutable after creation."
		},
		"name": {
			"type": "string",
			"description": "Resource name (letters, digits, underscores, must start with a letter). Combined with organizationName to form the qualified name '{organizationName}-{name}'. Immutable after creation."
		},
		"description": {
			"type": "string",
			"description": "Optional free-form description."
		},
		"overrides": {
			"type": "array",
			"description": "The per-column analyzer assignments — see ColumnAnalyzerOverrideEntry.",
			"items": {
				"$ref": "org.sagebionetworks.repo.model.search.table.ColumnAnalyzerOverrideEntry"
			}
		},
		"etag": {
			"type": "string",
			"description": "Synapse Optimistic Concurrency Control (OCC) value. Send the etag from a GET back on update; a mismatch returns 409."
		},
		"createdOn": {
			"type": "string",
			"format": "date-time",
			"description": "When this resource was created."
		},
		"createdBy": {
			"type": "string",
			"description": "ID of the user that created this resource."
		},
		"modifiedOn": {
			"type": "string",
			"format": "date-time",
			"description": "When this resource was last modified."
		},
		"modifiedBy": {
			"type": "string",
			"description": "ID of the user that last modified this resource."
		}
	}
}
