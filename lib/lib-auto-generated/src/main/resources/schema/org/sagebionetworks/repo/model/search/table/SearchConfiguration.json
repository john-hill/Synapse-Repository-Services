{
	"description": "A reusable search configuration resource. Bundles synonym sets, column analyzer overrides, and a default analyzer. Bind to any entity in the hierarchy (entity, folder, or project) to define inherited search configuration.",
	"properties": {
		"id": {
			"type": "string",
			"description": "The unique ID of this search configuration."
		},
		"organizationName": {
			"type": "string",
			"description": "The name of the Organization this resource belongs to. Immutable after creation."
		},
		"name": {
			"type": "string",
			"description": "The resource name. Must start with a letter and contain only letters, digits, and underscores. Unique within the organization and immutable after creation. Used as part of the qualified name ({organizationName}-{name}) when referenced by other resources."
		},
		"description": {
			"type": "string",
			"description": "Optional description."
		},
		"synonymSets": {
			"type": "array",
			"description": "Ordered list of SynonymSet qualified names in '{organizationName}-{name}' format to apply. Rules from all sets are unioned.",
			"items": {
				"type": "string"
			}
		},
		"columnAnalyzerOverrides": {
			"type": "array",
			"description": "Ordered list of ColumnAnalyzerOverride qualified names in '{organizationName}-{name}' format to apply. First-found-wins for duplicate columns.",
			"items": {
				"type": "string"
			}
		},
		"defaultAnalyzer": {
			"type": "string",
			"description": "Default text analyzer for columns without an explicit override, in '{organizationName}-{name}' format."
		},
		"etag": {
			"type": "string",
			"description": "Synapse employs an Optimistic Concurrency Control (OCC) scheme."
		},
		"createdOn": {
			"type": "string",
			"format": "date-time",
			"description": "The date this resource was created."
		},
		"createdBy": {
			"type": "string",
			"description": "The ID of the user that created this resource."
		},
		"modifiedOn": {
			"type": "string",
			"format": "date-time",
			"description": "The date this resource was last modified."
		},
		"modifiedBy": {
			"type": "string",
			"description": "The ID of the user that last modified this resource."
		}
	}
}
