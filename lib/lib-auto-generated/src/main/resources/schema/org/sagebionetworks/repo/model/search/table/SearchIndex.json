{
	"description": "A Synapse entity that materializes an OpenSearch index from a SQL view of a table-like entity. The 'definingSQL' picks the columns and rows to index; the SearchConfiguration (either pointed at directly via 'searchConfigurationId' or inherited from an ancestor binding) controls analyzers and synonyms. Once built, the index supports full-text search, faceted search, and autocomplete via the search endpoints on the SearchManagementController.\n\nPer-column mapping at index-build time:\n\n  Synapse column type                                  OpenSearch field type        ignore_above (keyword sub-field)\n  ---------------------------------------------------  ---------------------------  --------------------------------\n  STRING, STRING_LIST                                  text + keyword sub-field     1,000 chars\n  MEDIUMTEXT                                           text + keyword sub-field     100,000 chars\n  LARGETEXT                                            text + keyword sub-field     8,192 chars\n  LINK                                                 text + keyword sub-field     1,000 chars\n  ENTITYID, USERID, *_LIST id types                    keyword                      256 chars\n  INTEGER, DATE, INTEGER_LIST, DATE_LIST, FILEHANDLEID long                         n/a\n  DOUBLE                                               double                       n/a\n  BOOLEAN, BOOLEAN_LIST                                boolean                      n/a\n  JSON                                                 object (dynamic mapping)     n/a\n\nText / keyword values longer than the 'ignore_above' limit are NOT indexed for exact match or sort, but remain stored in the source document. Maximum 500,000 indexed rows per SearchIndex.",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.Entity"
		},
		{
			"$ref": "org.sagebionetworks.repo.model.table.HasDefiningSql"
		}
	],
	"properties": {
		"definingSQL": {
			"description": "The Synapse SQL statement that defines which columns and rows are indexed. Must reference exactly one entity (multi-entity JOINs are not supported). Each SELECT-list column becomes a field in the index — including literals and aliased expressions, which get a real ColumnModel id and behave as first-class columns.",
			"type": "string"
		},
		"searchConfigurationId": {
			"description": "Optional. ID of the SearchConfiguration to apply when building this index. If omitted, the build walks up the entity hierarchy (entity → folder → project) looking for the closest SearchConfigBinding; if none is found, platform defaults are used.",
			"type": "string"
		}
	}
}
