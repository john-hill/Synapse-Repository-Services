{
	"description": "Async request to query a SearchIndex entity's OpenSearch index. Identifies the target index and carries the search parameters.",
	"implements": [
		{
			"$ref": "org.sagebionetworks.repo.model.asynch.AsynchronousRequestBody"
		}
	],
	"properties": {
		"searchIndexId": {
			"type": "string",
			"description": "The ID of the SearchIndex entity to query."
		},
		"searchQuery": {
			"description": "The structured search query to execute against the index.",
			"$ref": "org.sagebionetworks.repo.model.search.SearchQuery"
		}
	}
}
