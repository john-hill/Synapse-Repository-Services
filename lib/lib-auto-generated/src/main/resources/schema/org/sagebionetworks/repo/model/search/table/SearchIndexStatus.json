{
	"description": "The build status of a SearchIndex's OpenSearch index.",
	"properties": {
		"searchIndexId": {
			"type": "string",
			"description": "The ID of the SearchIndex entity."
		},
		"state": {
			"description": "The current state of the SearchIndex's OpenSearch index.",
			"$ref": "org.sagebionetworks.repo.model.search.table.SearchIndexState"
		},
		"changedOn": {
			"type": "string",
			"format": "date-time",
			"description": "The date-time when the status last changed."
		},
		"errorMessage": {
			"type": "string",
			"description": "When the build fails, this is the error message."
		}
	}
}
